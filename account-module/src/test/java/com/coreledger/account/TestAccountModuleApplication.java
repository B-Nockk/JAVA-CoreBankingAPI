package com.coreledger.account;

import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class TestAccountModuleApplication {
    // exists only to satisfy @DataJpaTest's context scan
    // never runs in production
}